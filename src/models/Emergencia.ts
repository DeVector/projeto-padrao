class Emergencia {
  nome: string;
  numero: number;
  id: number;

  constructor(nome: string, numero: number, id: number){
    this.id = id;
    this.nome = nome;
    this.numero = numero;
  }

}

export default Emergencia;
